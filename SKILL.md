---
name: visual-language-extractor
description: Extract actionable visual DNA from image or video references, confirm fixed and replaceable layers, and create reusable photo or motion templates with faithful composition, typography, color, material, and layer relationships.
---

# Visual Language Template Extractor

把用户上传的图片或视频参考，转换成可重复使用的“视觉语言模板”。目标是：用户之后只需上传自己的照片（必要时是视频），即可尽可能 1:1 复刻参考素材的构图、字体、色彩、材质、层级和动效。不要只做抽象审美描述。

## 总原则

## 用户补充的禁止规则

- 不可以替换已经提取并确认的视觉元素；固定小人、线稿、箭头、文字、图标、照片关系、背景和组件必须原样保留，除非用户明确指定替换。
- 不可以改变复刻目标中的人物身份和人脸；目标图的人脸、五官、发型、肤色、姿态和身体轮廓必须保持原始像素，不得使用参考图人物身份。
- 参考图与复刻图必须严格分离：参考图只用于视觉语言、构图、色彩、材质和层级分析；目标图只用于目标主体及其身份像素来源；不得混淆、拼接或互换两者。
- 生成或合成前必须先确认参考图的真实图层顺序、元素清单、背景范围、遮挡关系，以及用户需要提交的图片和可编辑区域。
- 未完成上述确认前，不得生成最终复刻图；无法保持固定元素和目标像素时，必须停止并说明限制，不得用近似重绘结果冒充复刻。

## 视觉灵感卡片固定排版规则

- 最终视觉灵感卡片必须采用竖向长卡片版式。
- 顶部第一行固定为左右双栏对照：左侧放原始参考图，右侧放用户确认后的复刻图；两栏等宽、等高、等比例，并使用相同裁切规则。
- 参考图与复刻图只能各出现一次，不得混入目标原图、错误版本、额外参考图或局部截图；卡片排版不得改变两张图内部元素、文字、人物、背景、层级和遮挡关系。
- 顶部对照区下方固定使用十二宫格信息区。每格只承载一个已经提取并确认的主题，例如视觉语言、画布与构图、版式结构、照片插槽、字体、色彩、材质、图层关系、组件、动态规则、自动处理规则和可复用 Prompt。
- 十二宫格必须保留清晰的格子边界、标题和统一内边距；每格内容使用分点排版，不得把所有内容挤成一段长文。
- 每一行格子的高度必须按该行最长内容自动计算并统一对齐；内容较多时增加格子或卡片高度，不得删句、压缩、缩小到不可读或截断已确认的视觉语言。
- 卡片底部 Prompt 区的高度必须按实际文字行数自动撑开并收口，禁止预留大面积空白，也禁止为了压缩高度删改 Prompt 原文。
- 卡片自身的标题栏、网格线和文字排版只属于卡片容器，不得误加回顶部参考图或复刻图内部。
- 卡片内所有文字必须严格限制在所属格子或文本框的安全区域内，禁止任何文字出框、越过边界、被边框遮挡或被裁切。
- 文字换行必须按实际字体像素宽度计算，不能只按字符数量估算；每格必须保留统一的左右内边距、上下内边距和可读行高。
- 每个格子的高度必须根据换行后的实际行数自动撑开；内容较多时增加格子或卡片高度，不得缩小到不可读，也不得让文字溢出相邻格子。
- 生成或导出视觉灵感卡片前，必须逐格执行文字溢出检查，确认标题、正文、分点和 Prompt 均在边界内；发现出框、截断或遮挡时，必须先修正排版再交付。

- 先识别真实设计画布，再分析视觉语言；不要把平台录屏界面当成模板。
- 先确认模板结构，再生成最终复刻结果；没有确认不得直接生成最终模板。
- 保留原始照片中的人物身份、脸部、姿态和关键像素；不得擅自改脸、换人或改变主体身份。
- **身份来源硬规则：复刻图的人物身份只来自用户上传的目标/复刻图。参考图只能提供构图、版式、色彩、材质、字体、动效和装饰语言，绝不能提供脸、五官、发型、肤色、身体比例、身份特征或人物姿态。**
- **禁止脸部迁移：不得使用参考图人物脸作为生成条件或重绘目标，不得做 face swap、FaceID/身份嵌入、参考脸 IP-Adapter、整脸 inpaint、参考脸 landmark 驱动或任何会把参考人物五官复制到目标图的 img2img 流程。**
- 复刻默认采用“原始照片像素锁定 + UI/标注图层合成”：先把用户目标照片作为不可重绘的底图，再叠加参考模板中的窗口、文字、标注、裁切局部和装饰层。不得让图像生成模型重绘人物脸部、发型、皮肤、手部、服装、姿态或身体比例。
- **输入角色必须分离：`reference_image` 只能进入视觉语言分析/版式条件，`target_image` 只能进入人物像素与身份来源。不得把两者拼成一张条件图、互换变量、合并人脸区域或让参考图充当目标图。缺少 `target_image` 时只能分析模板，不得生成“复刻图”。**
- 对目标人物只允许不改变身份的裁切、等比缩放、排版和像素锁定合成；禁止对脸部做变形、重打光、美颜、磨皮、换肤、年龄化、风格化重绘或局部修补。
- 如果当前工具无法实现像素锁定的合成，必须明确说明无法保证身份保真，不得把整图重绘结果冒充合格复刻；应优先使用裁切、蒙版、Canvas/DOM 图层或其它合成方式。
- 先建立从后到前的真实图层顺序和遮挡关系，再做合成。不得用错误的几何遮罩替代人物轮廓。
- 不擅自新增黑边、按钮、卡片、标题栏、平台 UI、边框或装饰；只复刻参考中确实存在的元素。
- 若用户是在修改已有产物，只修改用户明确指出的局部，保留其它布局、DOM、参数、层级、动画和交互。
- 视觉语言提炼、复刻图生成、视觉灵感卡片是三个不同阶段；不得跳阶段、合并阶段或在复刻图未确认前生成卡片。
- **视觉语言提炼出的固定元素是锁定资产，不是可自由发挥的风格提示。** 必须逐项记录每个固定元素的原始来源、坐标/比例、层级、文字内容、数量和像素裁切；未经用户确认不得改字、改形、改比例、改位置、改颜色或重绘。
- **禁止自行重绘已提炼元素。** 参考中的手写字、线稿人物、梯子、箭头、贴纸、窗口、图标、配料照片和装饰必须优先以原始像素裁切、透明蒙版或图层合成复用；无法可靠提取原始资产时必须停止并说明缺少可复用原图，不能用手绘、字体替换、AI 生成或近似图补齐。
- **复刻图必须保留已确认元素。** 生成前建立“固定元素清单 + 原始资产清单 + 替换区域清单”，生成后逐项核对元素数量、文案、位置、尺寸、颜色、遮挡关系和像素来源；任一固定元素变化即判定失败并回退到原始像素合成。
- 参考图中的窗口、标题栏、按钮、图标、文件夹、播放器、标注、手写字、边框、贴纸和装饰是必须原样保留的固定素材，不能重绘、替换、补全、改文案、改图标或新增同类元素。
- 任何固定元素发生变化、缺失、位置漂移、数量变化或风格重做，都判定复刻失败；停止输出并回退到像素锁定合成。
- 对照卡片和案例仓库中的图片必须保留用户提供文件的原始像素、原始尺寸和原始格式；禁止为了排版另存 JPEG、降采样、截图、裁切或把 PNG 转成 JPEG。HTML/Markdown 只能用显示宽度控制展示尺寸，不能修改源文件。
- 如果手头只有截图、缩略图或已压缩副本，必须标明“不是原图”并要求重新提供原文件；不得把压缩副本冒充无损原图。
- 不把视频误称为 Live Photo。只有运行环境实际能输出可验证的 Live Photo 配对文件时，才能标注为 Live Photo；否则分别提供静态图、动态视频或转换所需的素材。

## 执行流程

### 1. 识别参考素材边界

先判断素材属于原始设计图、Instagram/小红书/相册截图、屏幕录制、带平台 UI 的录屏，或混合素材。明确写出：

- 真正的模板画布范围和比例；
- 录屏/平台 UI 范围；
- 需要排除的上下黑边、状态栏、返回/相机按钮、点赞/评论/分享、用户名、评论框、平台图标、录制提示和播放器控制区；
- 如果是录屏，先按内容边界裁切或在分析中排除外部 UI，并报告裁切后的画布比例。

不得把截图中的光标、浏览器栏、平台按钮或录制控制区域当作设计元素。

### 2. 提取可执行的视觉语言

按以下清单分析，使用可复刻参数，避免“高级感”“有氛围”“很有设计感”等空泛评价：

1. 画布比例、真实内容区域和安全边距；
2. 版式骨架、分栏比例、网格、对齐线和留白；
3. 固定元素的坐标、尺寸、间距、圆角、描边和阴影；
4. 用户照片插槽数量、形状、比例、层级和裁切方式；
5. 文字区、照片区、装饰区及其相互关系；
6. 字体类别、粗细、字号、字距、行距、大小写和对齐方式；
7. 背景色、主色、文字色、强调色及可给出的 HEX/RGB 近似值；
8. 色彩处理、对比度、曝光、饱和度、模糊、颗粒、噪点、透明度和混合模式；
9. 图像与文字的前后关系、穿插关系和遮挡关系；
10. 静态元素与动态元素的边界；
11. 动效的方向、路径、速度、加减速、错峰、循环、持续时间和首尾状态；
12. 输出到 Instagram Feed、Story、Reels 或 Live Photo 时的比例与安全区适配。

输出 5–10 个关键设计关键词。每个关键词必须包含：关键词名称、具体视觉规则、可复刻参数、可检索的英文关键词。关键词应直接服务于复刻或检索。

### 3. 拆分模板层级

将模板拆为四类，并明确每个元素的归属：

**A. 固定模板层**

画布比例、背景结构、版式骨架、字体系统、标题位置、照片插槽、圆角/描边/线条/装饰、色彩与材质规则、动效节奏和图层顺序。

**B. 用户上传区域**

背景照片数量、主照片数量、小照片数量、视频/Live Photo 支持情况、每张照片对应的插槽、插槽比例、裁切方式和主体位置要求。

**C. 用户可编辑内容**

标题、日期、月份、地点、关键词、字母矩阵、页码、照片数量和输出尺寸。参考中不存在的字段不要自行添加。

**D. 自动处理规则**

主体识别、背景清理、自动裁切、圆形/矩形蒙版、色彩统一、模糊/颗粒、字体排版、照片适配、动效生成和可验证的 Live Photo 配对输出。

### 4. 生成模板结构表

使用以下表格，不要省略用户上传区域与自动处理规则：

| 区域 | 固定内容 | 用户上传/编辑内容 | 自动处理规则 |
| --- | --- | --- | --- |
| 背景区 | 位置、比例、色彩、材质 | 用户背景图 | 裁切、调色、模糊 |
| 主视觉区 | 构图位置、尺寸、层级 | 主照片 | 主体识别、适配、保留原始像素 |
| 辅助照片区 | 插槽数量、形状、间距、前后顺序 | 多张小照片 | 自动排列、裁切、统一色彩 |
| 文字区 | 字体、字号、字距、层级、位置 | 用户文字（如允许） | 自动排版、换行和安全区检查 |
| 装饰区 | 线条、编号、图标、贴纸等 | 可选关键词（如允许） | 按固定坐标绘制，不新增元素 |
| 动效区 | 方向、路径、速度、循环、节奏 | 用户视频（如允许） | 按确认的规则生成动态版本 |

### 5. 视觉语言提取完成后输出“模板确认单”并等待确认

视觉语言提取完成后，必须先输出“视觉语言确认单”，等待用户明确确认；不得在这一阶段生成复刻图、生成卡片或调用整图重绘。确认单必须把参考图中将被锁定的固定元素逐项列出，并让用户确认或修改以下内容：

1. 真实模板画布与比例是否识别正确；
2. 哪些元素属于固定模板；
3. 哪些区域是用户上传照片/视频的插槽；
4. 哪些文字、日期、地点或数量可编辑；
5. 是否保留原始动效；
6. 输出静态图、短视频还是 Live Photo；
7. 是否需要 1:1、4:5、9:16 等尺寸；
8. 关键图层从后到前的顺序，以及人物与 UI 的遮挡关系。

确认单还必须明确写出：

- 固定元素清单（逐项列出坐标、比例、数量和是否原样合成）；
- 用户目标素材清单（哪些像素可以替换，哪些像素绝不能动）；
- 禁止项（改脸、改五官、改手、改人物姿态、重绘窗口/UI、增删元素、改文案、改变内部照片数量）；
- 复刻图生成方式（像素锁定合成，而不是整图生成）。

确认单必须增加“提炼元素锁定表”，至少包含：

| 元素 ID | 原始文件/裁切范围 | 固定内容 | 可否替换 | 复刻方式 |
| --- | --- | --- | --- | --- |
| E01… | 文件名、坐标、尺寸 | 原样保留的文字/线稿/照片/装饰 | 是/否 | 原始像素图层、透明蒙版或停止 |

用户必须明确确认这张锁定表后，才能进入复刻阶段。只说“风格类似”不视为确认；如果用户只确认了风格描述，没有确认元素清单，仍停留在提炼阶段。

在用户确认前只能分析和提问；如果用户未明确确认，必须停在此处。确认单只问必要问题；不要擅自引入复杂的草图、修改循环或额外页面。

### 6. 用户确认后生成

仅在用户确认视觉语言、提炼元素锁定表和模板确认单、并上传目标素材后生成复刻图。生成时必须先建立“参考图固定层 + 目标图像素层”的合成工程，再把已确认的固定元素按原坐标原样放回；不得用新字体、手绘、AI 重绘、近似图或整图生成替代任何固定元素。保持原始构图比例、照片插槽关系、字体层级、颜色、材质、图文距离、动效节奏和视觉重心。按用户需要提供：

- 模板预览图；
- 用户照片套用后的效果图；
- 固定层/可替换层标注图；
- 静态版本；
- 动态视频版本，或确实可验证的 Live Photo 版本；
- 可复用模板参数说明。

复刻图生成后必须同时展示“固定元素保留核对表”，列出每个 E01… 元素的原始来源与复刻结果状态；只能展示复刻图并询问用户是否需要修改。用户未明确表示“不需要修改”前，禁止制作视觉灵感卡片。

#### 6.3 复刻图身份保护硬门禁

生成复刻图时，必须把用户目标图作为人物像素的唯一来源，并按以下顺序执行：

1. 先锁定目标图中的人物区域和脸部区域；脸、眼睛、眉毛、鼻子、嘴唇、耳朵、发际线、痣/饰品和身体轮廓均标记为“不可生成、不可重绘”。
2. 参考图只作为视觉语言条件输入；在任何生成提示、参考图权重、控制图或图像编辑参数中，明确排除参考人物身份与五官。
3. 优先使用目标原图裁切 + 蒙版 + DOM/Canvas 图层合成。若必须调用图像生成能力，只生成背景、装饰或目标人物以外的空白区域，再把目标原始人物像素合回去。
4. 生成后逐项对照目标原图：双眼位置与大小、眉形、鼻梁/鼻翼轮廓、嘴唇形状、脸部轮廓、发际线和明显识别特征必须一致；任一项发生参考脸迁移、脸型变化、五官变化或身份不确定，立即判定失败并丢弃结果。
5. 失败时不得通过再次提高参考图相似度或继续 img2img 修补；必须回退到像素锁定合成路径，并向用户说明无法保证的部分。
6. 没有可验证的目标原图像素、人物锁定蒙版或前后对照结果时，默认判定为“不合格”，不得凭肉眼相似度放行。

生成调用必须带有等价于以下约束的负面规则：

> 只保留目标图人物身份与原始五官；禁止参考图人物脸、五官、脸型、发型、肤色、年龄、姿态和身份特征迁移；禁止 face swap、身份嵌入、整脸重绘、五官重绘和美颜改脸；参考图只用于版式、色彩、材质和装饰。

#### 6.2 最终视觉卡片的固定对照版式

最终视觉灵感卡片必须是竖向长卡片，不得输出横屏卡片。卡片顶部放一行等宽的双栏对照图，下面再排提炼内容：

**卡片标题与版式规则（提炼名称锁定，不擅自改写）**

- 卡片主标题必须沿用本次视觉语言提炼结果中已经确定的名称。不得在制作卡片时擅自换成泛化标题、重新命名、添加营销标题或替换为当前生成任务的临时标题。`复古桌面窗口 × 人像杂志拼贴`仅是本次示例提炼名称，不是所有项目的固定标题。
- 卡片顶部固定为双栏对照：左栏标题“参考图”，右栏标题“复刻图”。两栏必须在同一行、等宽、等高、等比例、使用相同裁切规则。
- 顶部双栏只能放原始参考图和用户确认的复刻图；不得混入目标照片、错误版本、额外参考图、局部截图、重复缩略图或重新绘制版本。
- 顶部双栏下方必须使用九宫格或十二宫格信息区。信息区按格子分点排列，每格一个主题，保持清晰标题栏与统一内边距，不得把所有内容挤成一段长文。
- 视觉灵感卡片默认使用白色或接近白色的底板、深色正文和清晰分隔线；除非用户明确指定深色版，不得自行改成黑色或深色底板。
- 九宫格或十二宫格只是内部排版结构，不得把“九宫格”“十二宫格”“9宫格”“12宫格”等布局术语写进卡片主标题、分区标题或正文，除非用户明确要求显示。
- 九宫格/十二宫格的主题只能来自本次已提炼并确认的视觉语言，优先使用：视觉语言描述、画布与构图、版式结构、照片插槽、字体系统、色彩体系、材质与质感、图层关系、组件形态、动态规则、自动处理规则、五个设计关键词。若使用九宫格，可合并相邻主题，但不得新增参考图中不存在的字段。
- 卡片内容必须严格复用已经确认的视觉语言、参数、组件名称、照片关系、字体层级、颜色、材质和图层顺序；制作卡片时不得为了排版方便改写成另一套视觉风格。
- 已提炼的视觉语言必须按原文完整写入对应信息格，不得压缩、摘要、删句、合并参数、缩写或同义改写。内容较多时应增加卡片高度、单元格高度或字号层级，不得以版面空间不足为由删减内容。
- 参考图和复刻图必须原样保留其内部元素、文字、窗口、人物、遮挡关系和裁切关系。卡片排版只能改变展示尺寸，不能编辑、擦除、重绘、替换、补全或美化两张图中的元素。
- 卡片自身可以有信息区的网格线、标题栏和文字排版，但不得把这些卡片容器误加回参考图或复刻图内部。

- 左栏只放一张最初用于提取视觉语言的原始参考图，标注“参考图”；
- 右栏只放一张用户确认的最终复刻图，标注“复刻图”；
- 两张图必须使用完全相同的展示宽度、高度、比例和裁切规则；
- 若用户明确指定“图2是复刻图”，右栏必须原样使用用户指定的图2，不得用其它生成结果替换、覆盖或重新绘制；
- 不得把原始目标照片、错误生成图、额外参考图或重复缩略图混入对照区；
- 右栏复刻图必须来自“原始目标照片像素锁定 + 视觉语言图层合成”，不能用重新绘制人物的整图生成结果替代；
- 下方内容区使用竖向分组排版，至少包含视觉语言描述、五个设计关键词、构图与版式、字体、色彩、材质、图层关系和可复用中文 Prompt。

#### 6.1 复刻图完成后的交互顺序（必须遵守）

复刻图生成完成后，不要直接宣称流程结束，也不要在复刻进行中把“生成视觉灵感卡片”当作当前步骤。必须按以下顺序交互：

1. 先展示复刻结果图，并明确说明“复刻图已生成”。
2. 询问用户：“复刻图是否需要修改？请指出要修改的具体位置或内容。”
3. 如果用户提出修改，只修改明确指出的局部，重新生成并再次询问是否需要修改；不得跳过复刻结果展示。
4. 只有用户明确表示“不需要修改”或等价确认后，才生成最终视觉灵感卡片。
5. 最终视觉灵感卡片必须包含已经确认的复刻结果图、视觉语言分析内容和可复用中文 Prompt。

如果复刻结果中出现任何参考图元素被重绘、人物身份漂移、固定 UI 被改写、窗口数量/位置变化、原图内部结构被拆开或画质被重新压缩，必须把它标记为失败版本，不能用“是否需要修改”来掩盖失败；应先回退并重新用原始像素合成。

如果用户没有要求复刻图，而只要求提取视觉语言，则继续遵守第 5 步的模板确认单流程，不自行生成复刻图。

如果用户只要求“提取视觉语言”，只输出视觉语言确认单，不生成复刻图或最终视觉灵感卡片；视觉灵感卡片必须等到复刻图生成并经用户明确确认后再制作。

## 视觉灵感卡片

在用户确认复刻图“不需要修改”后，默认生成一张最终“视觉灵感卡片”；只有用户明确表示不需要时才跳过。提取阶段可以准备卡片所需的视觉语言数据，但不要把卡片生成显示为复刻过程中的当前步骤。最终卡片必须在复刻结果确认后一次性补全，包含：

- 卡片标题必须与本次提炼出的视觉语言名称完全一致；不得在卡片成稿阶段自行改标题或改成抽象审美名称。若视觉语言名称尚未确认，先在模板确认单中标为待确认，不得自行命名。
- 顶部第一层为“参考图 / 复刻图”双栏对照，严格使用用户提供的原始参考图和已确认复刻图；两图等宽、等高、等比例、同裁切，且不改变图内任何元素。
- 顶部对照区下方为九宫格或十二宫格内容区。每格采用“主题标题 + 分点参数”的结构；每格只承载一个主题，主题数量不足时留白或合并相邻主题，不使用无关内容填充。
- 每一行格子必须根据该行最长内容自动计算高度，并以该行统一高度对齐；保留统一对齐的同时，明显减少格内空白。不得用整张卡片统一固定高度造成大面积空格。
- 每格只保留标题、完整原文、分点间距与必要安全边距所需的高度；不得通过删减、压缩、缩小正文或改变原文来降低格子高度。
- 推荐十二宫格字段顺序：①视觉语言描述，②画布与比例，③构图与版式，④版式结构，⑤照片插槽，⑥字体系统，⑦色彩体系，⑧材质与质感，⑨图层关系，⑩组件形态，⑪动态规则，⑫自动处理规则 / 五个设计关键词。若使用九宫格，必须保持同样的信息覆盖范围，通过合并相邻格实现。
- 每格内的内容必须从本次视觉语言提炼结果和模板确认单直接汇总，不得随意新增内容、删除关键参数、替换组件名称或把已确认的复刻语言改写成另一种风格。
- 每格必须保留对应提炼内容的原文和完整分点。若原文超过单格容量，应扩大单格或卡片，不得截断、概括或压缩。
- Prompt 区必须按照实际文字行数自动计算高度，保留必要的上下内边距；不得使用预设超高 Prompt 容器留下大面积底部空白，也不得为了收高而压缩 Prompt。
- 最终卡片画布高度必须跟随所有格子和 Prompt 的实际总高度自动收口，保留统一外边距后裁掉多余底部留白。
- 参考图与复刻图只允许作为顶部对照图出现一次；不得把它们再次拆成缩略图放入九宫格/十二宫格，除非用户明确要求做局部对照。

- 竖向卡片顶部的左侧参考图原图；
- 与左图等大、等比例的右侧复刻结果图（或动态版本的关键帧/缩略图）；
- 视觉语言描述；
- 五个最关键设计关键词及参数；
- 画面结构与版式比例；
- 前后层级、遮挡关系和主体保护规则；
- 组件形态与 UI 组件清单（仅列参考中真实存在的组件）；
- 字体系统；
- 色彩体系（含近似色值）；
- 材质、颗粒、噪点、模糊和透明度；
- 固定层、可替换层、可编辑字段和自动处理规则；
- 动效方向、节奏、持续时间和输出适配；
- 其它能帮助下次直接复刻的参数。

### 可复用 Prompt 的语言规则

- 可复用 Prompt 必须使用中文书写，供用户直接复制后再次生成同款视觉结果。
- Prompt 必须具体写出画布比例、构图位置、照片插槽、字体层级、颜色、材质、图层顺序、人物保护、动效（如有）和禁止项。
- 最终视觉灵感卡片中的可复用 Prompt 必须逐字保留提炼阶段已经确认的 Prompt 原文，不得压缩、摘要、删句、缩写、合并段落或同义改写。排版空间不足时必须增加卡片高度或 Prompt 区域高度。
- 不得只输出英文 Prompt、英文模板描述或无法执行的审美形容词。
- 如需提供英文检索词，只能作为独立的关键词辅助字段，不能替代中文 Prompt。

卡片必须同时保留原图与视觉语言复刻结果，便于用户下次直接调用同一模板。不要把平台截图控件或录制黑边保存为视觉语言。

### 视觉灵感卡片固定排版（不可变）

视觉灵感卡片只在复刻图获得用户明确确认后制作，并且固定为一张竖向长卡片：

1. 顶部同一行放两个等宽等高栏位：左栏“原图”，右栏“复刻图”。左栏只放最初的完整原始参考图，右栏只放用户确认的完整复刻图；两张图内部不拆分、不裁切、不重绘、不重新压缩。
2. 顶部对照图下方放视觉语言提取内容，使用固定网格分组呈现视觉语言、画布/构图、版式、字体、色彩、材质、图层关系、组件和自动处理规则。每格只放已确认内容，不把额外图片、目标原图或错误版本塞进卡片。
3. 对照区只出现这一组原图/复刻图各一次；不得把一张图拆成上下缩略图，也不得把同一组误写成两个案例。
4. 卡片排版只能改变显示尺寸，不能改变源文件字节、尺寸、格式、内部元素、文字、人物、窗口数量或遮挡关系；优先使用原始文件链接或像素锁定合成结果。
5. 如果无法在保持源文件和图层关系的前提下生成卡片，必须说明限制并停下，不得用低清截图、重新渲染图或近似重绘图替代。

## 复刻前的层级检查

在输出前逐项核对：

- 背景、装饰、UI、照片、人物、前景元素的真实前后顺序；
- 每个插槽是否使用正确的用户素材；
- 人物脸部和原始像素是否保持；
- 文字是否压住不该压住的照片；
- 是否意外新增黑边、按钮、标题栏、卡片或边框；
- 动效首帧、运动方向、速度和结束状态是否与参考一致。

若发现无法可靠判断的区域，标为“待确认”，不要猜测或自行发挥。

## 能力边界与诚实输出

- 当前 Skill 在 Codex 中依赖可用的视觉理解与图像生成能力，不要求用户提供外部 GPT API key；不要声称已经连接不存在的外部后端。
- 若当前环境不能直接导出 Live Photo，明确说明限制，并提供静态图、视频或 Live Photo 转换所需的配对素材，不把 MOV/MP4 冒充 Live Photo。
- 对版权、身份和平台 UI 只复刻用户提供且有权使用的内容；对无法辨认的字体或图案给出近似方案并标注不确定性。
